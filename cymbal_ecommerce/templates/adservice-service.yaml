apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name }}-{{ .Values.adservice.name }}-service"
  namespace: "{{ .Release.Name }}-{{ .Values.namespace }}-{{ .Values.environment }}"
spec:
  type: ClusterIP
  selector:
    {{- include "common_labels" . | nindent 4 }}
    {{- include "adservice_labels" . | nindent 4 }}
  ports:
  - name: grpc
    port: {{ .Values.adservice.service.port }}
    targetPort: {{ .Values.adservice.service.target_port }}