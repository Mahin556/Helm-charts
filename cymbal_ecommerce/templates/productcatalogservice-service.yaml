apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name }}-{{ .Values.productcatalogservice.name }}-service"
  namespace: "{{ .Release.Name }}-{{ .Values.namespace }}-{{ .Values.environment }}"
spec:
  type: ClusterIP
  selector:
    {{- include "common_labels" . | nindent 4 }}
    {{- include "productcatalogservice_labels" . | nindent 4 }}
  ports:
  - name: grpc
    port: {{ .Values.productcatalogservice.service.port }}
    targetPort: {{ .Values.productcatalogservice.service.target_port }}