apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name }}-{{ .Values.shippingservice.name }}-service"
  labels:
    {{- include "common_labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "common_labels" . | nindent 4 }}
    {{- include "shippingservice_labels" . | nindent 4 }}
  ports:
  - name: grpc
    port: {{ .Values.shippingservice.service.port }}
    targetPort: {{ .Values.shippingservice.service.target_port }}
