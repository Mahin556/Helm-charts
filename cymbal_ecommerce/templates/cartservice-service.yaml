apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name }}-{{ .Values.cartservice.name }}-service"
  namespace: "{{ .Release.Name }}-{{ .Values.namespace }}-{{ .Values.environment }}"
spec:
  type: ClusterIP
  selector:
    app: {{ .Values.app_name }}
    service: {{ .Values.cartservice.name }}
    environment: {{ .Values.environment }}
    release: {{ .Release.Name }}
  ports:
  - name: grpc
    port: {{ .Values.cartservice.service.port }}
    targetPort: {{ .Values.cartservice.service.target_port }}
