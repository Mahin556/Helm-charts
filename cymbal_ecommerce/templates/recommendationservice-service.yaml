apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name }}-{{ .Values.recommendationservice.name }}-service"
  labels:
    {{- include "common_labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "common_labels" . | nindent 4 }}
    {{- include "recommendationservice_labels" . | nindent 4 }}
  ports:
  - name: grpc
    port: {{ .Values.recommendationservice.service.port }}
    targetPort: {{ .Values.recommendationservice.service.target_port }}
