apiVersion: apps/v1
kind: Deployment
metadata:
  name: "{{ .Release.Name }}-{{ .Values.productcatalogservice.name }}-deployment"
  namespace: "{{ .Release.Name }}-{{ .Values.namespace }}-{{ .Values.environment }}"
spec:
  replicas: {{ .Values.productcatalogservice.pod.replicas }}
  selector:
    matchLabels:
      {{- include "common_labels" . | nindent 6 }}
      {{- include "productcatalogservice_labels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "common_labels" . | nindent 8 }}
        {{- include "productcatalogservice_labels" . | nindent 8 }}
    spec:
      serviceAccountName: default
      terminationGracePeriodSeconds: 5
      securityContext:
        fsGroup: 1000
        runAsGroup: 1000
        runAsNonRoot: true
        runAsUser: 1000
      containers:
      - name: "{{ .Release.Name }}-{{ .Values.productcatalogservice.name }}-container"
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
              - ALL
          privileged: false
          readOnlyRootFilesystem: true
        image: "{{ .Values.productcatalogservice.container.image.name }}:{{ .Values.productcatalogservice.container.image.tag }}"
        ports:
        - containerPort: {{ .Values.productcatalogservice.container.port }}
        env:
        - name: PORT
          value: "{{ .Values.productcatalogservice.container.port }}"
        - name: DISABLE_PROFILER
          value: "{{ .Values.productcatalogservice.container.disable_profiler }}"
        readinessProbe:
          grpc:
            port: {{ .Values.productcatalogservice.container.port }}
        livenessProbe:
          grpc:
            port: {{ .Values.productcatalogservice.container.port }}
        resources:
          requests:
            cpu: 100m
            memory: 64Mi
          limits:
            cpu: 200m
            memory: 128Mi