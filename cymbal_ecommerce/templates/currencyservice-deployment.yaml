apiVersion: apps/v1
kind: Deployment
metadata:
  name: "{{ .Release.Name }}-{{ .Values.currencyservice.name }}-deployment"
  namespace: "{{ .Release.Name }}-{{ .Values.namespace }}-{{ .Values.environment }}"
spec:
  replicas: {{ .Values.currencyservice.pod.replicas }}
  selector:
    matchLabels:
      {{- include "common_labels" . | nindent 6 }}
      {{- include "currencyservice_labels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "common_labels" . | nindent 8 }}
        {{- include "currencyservice_labels" . | nindent 8 }}
    spec:
      serviceAccountName: default
      terminationGracePeriodSeconds: 5
      securityContext:
        fsGroup: 1000
        runAsGroup: 1000
        runAsNonRoot: true
        runAsUser: 1000
      containers:
      - name: "{{ .Values.currencyservice.name }}-container"
        securityContext:
          allowPrivilegeEscalation: false
          capabilities:
            drop:
              - ALL
          privileged: false
          readOnlyRootFilesystem: true
        image: "{{ .Values.currencyservice.container.image.name }}:{{ .Values.currencyservice.container.image.tag }}"
        ports:
        - name: grpc
          containerPort: {{ .Values.currencyservice.container.port }}
        env:
        - name: PORT
          value: "{{ .Values.currencyservice.container.port }}"
        - name: DISABLE_PROFILER
          value: "{{ .Values.currencyservice.container.disable_profiler }}"
        readinessProbe:
          grpc:
            port: {{ .Values.currencyservice.container.port }}
        livenessProbe:
          grpc:
            port: {{ .Values.currencyservice.container.port }}
        resources:
          requests:
            cpu: 100m
            memory: 64Mi
          limits:
            cpu: 200m
            memory: 128Mi