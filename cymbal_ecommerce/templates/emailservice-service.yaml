apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name }}-{{ .Values.emailservice.name }}-service"
  namespace: "{{ .Release.Name }}-{{ .Values.namespace }}-{{ .Values.environment }}"
spec:
  type: ClusterIP
  selector:
    {{- include "common_labels" . | nindent 4 }}
    {{- include "emailservice_labels" . | nindent 4 }}
  ports:
  - name: grpc
    port: {{ .Values.emailservice.service.port }}
    targetPort: {{ .Values.emailservice.service.target_port }}
