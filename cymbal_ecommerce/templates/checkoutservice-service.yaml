apiVersion: v1
kind: Service
metadata:
  name: "{{ .Release.Name }}-{{ .Values.checkoutservice.name }}-service"
  namespace: "{{ .Release.Name }}-{{ .Values.namespace }}-{{ .Values.environment }}"
spec:
  type: ClusterIP
  selector:
    {{- include "common_labels" . | nindent 4 }}
    {{- include "checkoutservice_labels" . | nindent 4 }}
  ports:
  - name: grpc
    port: {{ .Values.checkoutservice.service.port }}
    targetPort: {{ .Values.checkoutservice.service.target_port }}
