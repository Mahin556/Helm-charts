{{- if .Values.externalSecrets.enabled }}
{{- if .Values.externalSecrets.secrets.appCredentials.enabled }}
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: '{{ include "gowebapp.fullname" . }}{{ .Values.externalSecrets.secrets.appCredentials.suffix }}'
  namespace: '{{ .Release.Namespace }}'
  labels:
    {{- include "gowebapp.labels" . | nindent 4 }}
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  refreshInterval: '{{ .Values.externalSecrets.refreshInterval }}'
  secretStoreRef:
    name: '{{ .Values.externalSecrets.store.name }}'
    kind: '{{ .Values.externalSecrets.store.kind }}'
  target:
    name: '{{ include "gowebapp.fullname" . }}{{ .Values.externalSecrets.secrets.appCredentials.suffix }}'
    creationPolicy: Owner
  data:
    {{- range .Values.externalSecrets.secrets.appCredentials.keys }}
    - secretKey: {{ .secretKey }}
      remoteRef:
        key: {{ $.Values.externalSecrets.secrets.appCredentials.vaultPath }}
        property: {{ .property }}
    {{- end }}
{{- end }}
{{- end }}